# Portable packet fixtures

These language-neutral Markdown fixtures freeze Tandem's packet envelope and portable YAML profile. `manifest.json` records whether each fixture is valid and the expected normalized problem path for invalid input. Both the C# and TypeScript packet suites consume this manifest and these exact files.
